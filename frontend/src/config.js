// API Configuration
// Uses a relative path so all requests go through nginx on the same origin.
// nginx routes /meal_predict/api/v1/* → backend:8001/api/v1/*

export const API_BASE_URL = '/smartcatering/api/v1';
